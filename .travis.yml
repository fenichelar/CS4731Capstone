---
language: node_js
node_js:
  - "0.12.15"

sudo: false

cache:
  directories:
    - node_modules

before_install:
  - npm config set spin false
  - npm install -g npm@^2

install:
  - npm install

before_script:
  - npm lint

script:
  - npm test
