---
language: node_js
node_js:
  - "0.12.15"

sudo: false

branches:
  only:
    - master

cache:
  directories:
    - node_modules

before_install:
  - npm config set spin false
  - npm install -g npm@^2

install:
  - npm install

before_script:
  - npm run lint

script:
  - npm test
